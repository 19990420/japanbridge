# japanbridge

ARKLinks（ark-links.com）のランディングページ（静的HTML、`index.html`）。2026-07 のEC先行再編に伴い、茶の海外EC（一軒の農園から、顧客の好みに合わせて三種を選ぶ・DDP・米国のみ）向けの構成。⚠️**抹茶は2026-08-25に削除（初回販売しない）。**旧・紹介制プライベートツアー版は `legacy/tour-index_2026-07.html` に保全（冬のL2再起動時に再利用）。

- ⚠️ **`/farms/moriuchi/` は 2026-08-25 に削除した。** 農園名を出す前提・農園訪問の約束・農園で配るQRカードという、いずれも撤回済みの方針で書かれていたため。`/farm` `/farms` `/farms/moriuchi/` は `vercel.json` で `/#farm` へ301。**⚠️ 生産者ページを再び作る場合は新規に書くこと。あのファイルを復元しない**
- `#farm` アンカーはトップページ内の農園セクション。旧QRの着地点だったため、アンカー名は変更しない
- ⚠️ **2026-09-02：箱の中身を「和紅茶30g＋ほうじ茶100g＋煎茶100g（3つから1つ選択）」に固定した。**旧「蒸し製／火入れ／おまかせ」の3スロットと**玄米茶は全廃**。連動して直したのは `#tea` の3スロット・`.checkout-unit`（230g・約55杯）・FAQ・meta keywords・全CTA（`Choose your three`→`Choose your tea`）・`/brewing` の表。**⚠️ 中身を変えるときは必ずこの6箇所を揃えること**（Shopifyのバリアントは煎茶の3値だけ）
  - `.slot-his`（＝農家が選ぶ枠）は `.slot-pick`（＝客が選ぶ枠）に改名。強調される枠が入れ替わったため
  - ⚠️ `.slot-opts` の `min-height:128px` は **901px以上でのみ**掛ける。01・02は選択肢1行／03は3行なので、揃えないと説明文の開始位置が段違いになる。⭐1列に畳む幅では逆に空白になるので掛けない
  - ⚠️ `/brewing` の表から**茎茶・釜炒りの行を削除**した（箱に入る経路が消えたため。袋には `GREEN TEA` としか書かれず、該当しない緑茶の行があると「自分のはどれか」が分からなくなる）。**数値はHTMLコメントに残してあるので、おまかせ枠を戻すときは復活させる**
- Foundingフォームは暫定でFormspree送信。9月のIG発信開始前にESP（Klaviyo/Shopify Email）直結へ差し替える
- **`#fields`（The Fields）は2026-08-24追加。** 8/23の森内訪問で代表が撮影した4枚（`fields_valley`＝谷の全景／`fields_returning`＝刈るのをやめた境目／`fields_kept_apart`＝覆いのある畑と日向の畑／`fields_two_pickings`＝隣り合う畝の緑の差）。⚠️ **本文は「安全版」**——斜面の地形と山間茶業の一般的な衰退（いずれも公知）だけを書き、**近隣の特定の農家について一切言及していない**。「隣人が辞めると土地が売れないので、まだ働いている人に回る」という直接的な版は `admin/ARKLinks_経営目標.md` §v5.0(2) に保管してあり、**同じ谷の他の家の話であるため、森内さんの明示的な同意を得るまで公開してはならない**（該当箇所にHTMLコメントあり）
- ⚠️ **2026-08-25：サイト上の茶畑写真をすべて森内茶農園（2026-08-23撮影）に統一した。** それ以前の写真は和束（京都）や静岡の**別の農園**であり、「静岡の一軒の農園から」という本文と矛盾していたためリポジトリから削除済み（`tea_rows_sky` `tea-fields` `茶畑` `お茶` `tea.jpg` ほかストック画像）。**今後、森内茶農園以外で撮影した茶畑写真をこのサイトに置かないこと。**
  - Hero=`hero_tea_hills`（斜面いっぱいの茶畑）／OG=`og_image.jpg`（谷の全景）／`farm_shizuoka`（覆いの棚＝かぶせ栽培）／`sencha_fields`（茶葉の接写）／`fields_*` 4枚は `#fields` 用
  - ⚠️ `legacy/tour-index_2026-07.html` が参照する `tea_fields.webp` `tea.webp` `rice.webp` `winery.webp` `sake_brewery.webp` は**他所の写真だが、アーカイブを壊さないため残置**。L2は畳む決定済みなので、legacy を破棄する際に併せて削除してよい
  - マスターは同名`.jpg`。農園名と人物写真は覚書後に掲載

## 起動方法
`index.html` をブラウザで直接開くか、簡易サーバーで確認する（例: `python3 -m http.server`）。

## 画像命名規則
新規に追加する画像は英語のスネークケース（例: `sake_brewery.jpg`）で統一する。日本語ファイル名（`お米.jpg` 等）と英語ファイル名の重複が既存であるため、新規追加時は増やさない。

## 画像最適化ルール
サイトで表示する画像は WebP に変換して使う（Pillow）。⚠️ **2026-08-25 改定：茶葉の写真は細密で圧縮が効かないため、旧ルール（ヒーロー 幅2000px/品質72）では800KB超になり `vercel-seo-check` の上限400KBを大きく超える。⭐ 品質を落とすとブロックノイズが出るので、下げるのは品質ではなく寸法。** 実測でうまくいった設定：**ヒーロー=幅1440px/品質54（約390KB）／全幅の本文画像=幅1700-1800px/品質62（約310KB）／グリッド内の画像=幅950px/品質70-72（約120-240KB）**。上限はヒーロー400KB・本文350KB。元のjpg/pngはマスターとしてリポジトリに残すが、HTMLからは参照しない。OG画像（`og_image.jpg`）のみ互換性のためJPEGを維持する。ファーストビュー外の`<img>`には `loading="lazy" decoding="async"` を付ける。

## デプロイ先
Vercel（GitHub連携）。`origin`（github.com/19990420/japanbridge）への push で自動デプロイされる。**push は代表の明示的な承認を得てから行うこと（本番サイトが即時更新されるため）。**
